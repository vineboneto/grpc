version: "3.7"

services:
  grpc:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grpc
    command: >
      /bin/bash -c "
        yarn build &&
        yarn start:server
      "
    # restart: always
